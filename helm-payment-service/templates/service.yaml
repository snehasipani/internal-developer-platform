apiVersion: v1
kind: Service

metadata:
  name: payment-service
  namespace: payment

spec:
  type: ClusterIP

  selector:
    app: payment-service

  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP